package com.example.brs.service.impl;

import com.example.brs.dto.entity.bus.TicketDto;
import com.example.brs.dto.entity.bus.TripScheduleDto;
import com.example.brs.dto.entity.user.UserDto;
import com.example.brs.service.TicketService;

public class TicketServiceImpl implements TicketService {
    /**
     * @param tripScheduleDto
     * @param passenger
     * @return
     */
    @Override
    public TicketDto bookTicket(TripScheduleDto tripScheduleDto, UserDto passenger) {
        return null;
    }
}
